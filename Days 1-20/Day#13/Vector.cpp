#include <iostream>
#include <vector>

int main()
{
    std::vector<int> numbers{}; // creating a empty vector
    std::vector<int> PrimeNums{};

    int PrimeNumList;

    numbers.push_back(1);

    for(int num : numbers)
    {
        std::cout << num;
    }

    // I want to make a program what checks if a number is a prime and if it is its stored in a vector array



}